//program to find minimum of two numbers
public class MinOfTwo {
    
    public static void minimumNumber(integer a , integer b){
        
        if (a<b){
            system.debug('Number '+ a+' is smller to '+b);
        }
        
        else{
            system.debug('Number ' +b+ ' is smaller to '+a);
        }
    }

}
